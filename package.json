{
  "name": "uni",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=18.20.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name cmmi-dmm --branch main",
    "sync": "astro build && wrangler pages deploy dist --project-name cmmi-dmm --branch main && git add . && git commit -m 'update: nuevas evidencias y ajustes' && git push",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.3",
    "bpmn-js": "^18.29.1",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  }
}